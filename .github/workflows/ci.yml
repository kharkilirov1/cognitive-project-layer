name: CI

on:
  push:
    branches: [main, master]
  pull_request:

jobs:
  test:
    name: Test (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest]

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Cache cargo
        uses: Swatinem/rust-cache@v2

      - name: Format check
        run: cargo fmt --check

      - name: Test
        run: cargo test

      - name: Clippy
        run: cargo clippy --all-targets -- -D warnings

      - name: Build binaries
        run: cargo build --bins

      - name: SQLite index smoke
        run: |
          cargo run --quiet --bin cpl -- --root . index-build
          cargo run --quiet --bin cpl -- --root . index-db --json

      - name: Python script syntax
        run: python -m py_compile scripts/eval_retrieval.py scripts/bench_cli.py scripts/bench_mcp.py

      - name: Retrieval eval
        run: python scripts/eval_retrieval.py --top-k 3

      - name: CLI benchmark smoke
        run: python scripts/bench_cli.py --iterations 1 --warmup 0

      - name: MCP benchmark smoke
        run: python scripts/bench_mcp.py --iterations 1 --warmup 0
